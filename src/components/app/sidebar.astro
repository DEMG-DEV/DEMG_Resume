---
---
<aside class="hidden md:flex w-[280px] h-full flex-col bg-white/40 backdrop-blur-3xl border-r border-white/20 select-none">
    <!-- Window Traffic Lights (Simulated) -->
    <div class="h-14 flex items-center px-5 gap-2">
        <div class="w-3 h-3 rounded-full bg-[#FF5F57] shadow-sm"></div>
        <div class="w-3 h-3 rounded-full bg-[#FEBC2E] shadow-sm"></div>
        <div class="w-3 h-3 rounded-full bg-[#28C840] shadow-sm"></div>
    </div>

	<!-- User Profile / Branding -->
	<div class="px-5 mb-8 flex items-center justify-between">
		<div class="flex items-center gap-3">
			<img src="/images/logo.png" alt="Profile" class="w-10 h-10 rounded-full shadow-md object-contain bg-white/50" />
			<div class="flex flex-col">
				<span class="font-bold text-sm leading-tight">DEMG-DEV</span>
				<span class="text-xs text-secondary font-medium">Architect</span>
			</div>
		</div>

        <!-- Mini Theme Toggle -->
        <button id="theme-toggle" class="p-2 rounded-full hover:bg-black/5 text-secondary transition-colors" aria-label="Toggle Theme">
            <!-- Sun Icon (for light mode) -->
            <svg id="sun-icon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path></svg>
            <!-- Moon Icon (for dark mode) -->
            <svg id="moon-icon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path></svg>
        </button>
	</div>

	<!-- Navigation -->
	<nav class="flex-1 px-3 overflow-y-auto hide-scrollbar text-[13px] font-medium text-secondary">
		
            <div class="mb-6">
                <ul class="space-y-1">
                    <li>
                        <a href="#about" class="flex items-center gap-3 px-3 py-2 text-primary-custom hover:bg-black/5 rounded-lg transition-colors group">
                            <span class="w-8 h-8 flex items-center justify-center rounded-md bg-blue-500 text-white shadow-sm group-hover:scale-105 transition-transform">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                            </span>
                            <span class="font-medium">About Me</span>
                        </a>
                    </li>
                    <li>
                        <a href="#projects" class="flex items-center gap-3 px-3 py-2 text-primary-custom hover:bg-black/5 rounded-lg transition-colors group">
                            <span class="w-8 h-8 flex items-center justify-center rounded-md bg-purple-500 text-white shadow-sm group-hover:scale-105 transition-transform">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path></svg>
                            </span>
                            <span class="font-medium">Projects</span>
                        </a>
                    </li>
                    <li>
                        <a href="#skills" class="flex items-center gap-3 px-3 py-2 text-primary-custom hover:bg-black/5 rounded-lg transition-colors group">
                            <span class="w-8 h-8 flex items-center justify-center rounded-md bg-orange-500 text-white shadow-sm group-hover:scale-105 transition-transform">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                            </span>
                            <span class="font-medium">Skills</span>
                        </a>
                    </li>
                    <li>
                        <a href="#work" class="flex items-center gap-3 px-3 py-2 text-primary-custom hover:bg-black/5 rounded-lg transition-colors group">
                            <span class="w-8 h-8 flex items-center justify-center rounded-md bg-green-500 text-white shadow-sm group-hover:scale-105 transition-transform">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                            </span>
                            <span class="font-medium">Experience</span>
                        </a>
                    </li>
                    <li>
                        <a href="#contact" class="flex items-center gap-3 px-3 py-2 text-primary-custom hover:bg-black/5 rounded-lg transition-colors group">
                            <span class="w-8 h-8 flex items-center justify-center rounded-md bg-gray-700 text-white shadow-sm group-hover:scale-105 transition-transform">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                            </span>
                            <span class="font-medium">Get in Touch</span>
                        </a>
                    </li>
                </ul>
            </div>

<script is:inline>
    function initTheme() {
        const themeToggle = document.getElementById('theme-toggle');
        const sunIcon = document.getElementById('sun-icon');
        const moonIcon = document.getElementById('moon-icon');
        const themeText = document.getElementById('theme-text');
        const html = document.documentElement;

        // Check for saved theme preference, otherwise use system preference
        const savedTheme = localStorage.getItem('theme');
        const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'business' : 'corporate';
        const currentTheme = savedTheme || systemTheme;

        // Apply initial theme
        html.setAttribute('data-theme', currentTheme);
        updateUI(currentTheme);

        // Remove old event listener if exists (to prevent duplicates if re-running) by cloning or just adding fresh
        // For inline script, it runs once on load.
        
        themeToggle?.addEventListener('click', () => {
            const isDark = html.getAttribute('data-theme') === 'business';
            const newTheme = isDark ? 'corporate' : 'business';
            
            html.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateUI(newTheme);
        });

        function updateUI(theme) {
            if (theme === 'business') {
                html.classList.add('dark');
                sunIcon?.classList.remove('hidden');
                moonIcon?.classList.add('hidden');
                if(themeText) themeText.textContent = 'Light Mode';
            } else {
                html.classList.remove('dark');
                sunIcon?.classList.add('hidden');
                moonIcon?.classList.remove('hidden');
                if(themeText) themeText.textContent = 'Dark Mode';
            }
        }
    }

    // Run on load
    document.addEventListener('DOMContentLoaded', initTheme);
    // Also run on Astro view transition if applicable (optional but good practice)
    document.addEventListener('astro:page-load', initTheme); 
</script>



    </nav>
    
    <!-- Footer / Branding -->
    <div class="p-4 border-t border-white/20 text-[11px] text-center text-secondary font-medium">
        © 2026 DEMG-DEV
    </div>
</aside>

<style>
    /* Custom Scrollbar for Sidebar */
    .hide-scrollbar::-webkit-scrollbar {
        width: 0px;
        background: transparent;
    }
</style>
